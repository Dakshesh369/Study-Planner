# Optional. If set, the AI summary/chat use Claude directly.
# If left blank, the app runs fully in "offline coach mode" with
# solid rule-based fallbacks — no functionality is lost, just the
# AI text becomes templated instead of freshly generated.
ANTHROPIC_API_KEY=
